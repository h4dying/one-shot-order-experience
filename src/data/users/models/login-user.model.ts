/**
 * The user login request data-model.
 */
export interface LoginUserInput {
  /**
   * Gets or sets the email of the user.
   */
  email: string;

  /**
   * Gets or sets the plain password of the user.
   */
  password: string;
}
